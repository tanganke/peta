name: SUN397

datamodule:
  _target_: peta.datamodules.SUN397DataModule
  root: data
  batch_size: ${batch_size}
  num_workers: ${num_workers}
  pin_memory: false
  train_transform: null
  test_transform: null
